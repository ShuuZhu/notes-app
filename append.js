const fs = require('fs')
const moment = require('moment')

// fs['writeFileSync']('test.txt', 'this is teset');

// fs.appendFileSync('test.txt', 'this is append test')

// let s = fs.statSync('test.txt')
// fs.stat('test.txt', (err, stat) => {
//   const now = moment().valueOf()
//   console.log((now - stat.birthtimeMs) / 60 / 60 / 1000 / 24)
// })

// fs.unlinkSync('test.txt')
